/** @type {import('next').NextConfig} */
const nextConfig = {
  browserslist: [
    '> 0.5%',
    'not dead',
    'ios >= 10',
    'chrome >= 49',
    'not ie <= 11',
  ],
}

module.exports = nextConfig
